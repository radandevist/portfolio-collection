{
  "name": "portfolio-factory",
  "version": "1.0.0",
  "private": true,
  "description": "Portfolio variations factory - generate and compare multiple portfolio designs",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r --parallel dev",
    "clean": "pnpm -r exec rm -rf node_modules build .react-router"
  },
  "keywords": ["portfolio", "react-router", "tailwindcss", "pnpm-workspace"],
  "author": "iamradan",
  "license": "MIT",
  "engines": {
    "node": ">=20",
    "pnpm": ">=8"
  }
}
